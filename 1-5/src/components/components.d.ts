type Route = {
    path: string,
    name: string
}

type User = {
    id: number,
    name: string,
    icon: string

}

type Product = {
    name: string,
    picture: string,
    description: string,
    quantity: number
}

type Task = {
    content: string,
    checked: boolean
}